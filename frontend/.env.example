# URL de l'API backend en production.
# En développement, laisser vide : le proxy Vite redirige /api vers http://localhost:3000.
# En production (build statique), définir l'URL publique du backend, ex :
# VITE_API_URL=https://edpulse-api.onrender.com
VITE_API_URL=
